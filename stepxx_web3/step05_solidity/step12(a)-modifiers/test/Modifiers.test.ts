import { expect } from "chai";
import { ethers } from "hardhat";

describe("Modifiers", function () {
  it("should allow the owner to update the owner address", async function () {
    // const [owner, newOwner] = await ethers.getSigners();
    // const ModifiersContract = await ethers.getContractFactory("modifiers");
    // const modifiers = await ModifiersContract.connect(owner).deploy();
    // await modifiers.connect(owner).updateOwner(newOwner.address);
    // expect(await modifiers.owner()).to.equal(newOwner.address);
  });
});
